To get working 
1) Have lua installed
2) `git clone git@github.com:wba6/primeConfig.neo.git ~/.config/nvim`
3) Start neovim - lazy.nvim will automatically bootstrap and install all plugins on first launch
